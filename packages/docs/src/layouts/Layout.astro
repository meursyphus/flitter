---
import Header from "../components/ui/Header.astro";
interface Props {
  title?: string;
  description?: string;
  image?: string;
}

const {
  image = "/og.png",
  title = "Flitter - Say Goodbye to Chart Libraries, Hello to Freedom!",
  description = "🔥 No more limits. 🎨 Declare, render, and customize your way. 🖌️ SVG or Canvas, your rules. 🚀 Built for high-performance visualizations.",
} = Astro.props;

// Set the base URL for canonical links
const BASE_ORIGIN = "https://flitter.dev";
const fullCanonicalUrl = `${BASE_ORIGIN}${Astro.url.pathname}`;

const themeScript = `
    (() => {
         try {
           let savedTheme = localStorage.getItem('theme');
           if(!savedTheme) {
             localStorage.setItem('theme', 'dark');
             savedTheme = 'dark';
           }
           if(savedTheme === 'light') {
             document.documentElement.classList.remove('dark');
           } else {
             document.documentElement.classList.add('dark'); 
           }
        } catch (e) {
           console.error('Theme script error:', e); 
        } 
    })();
  `;
---

<!doctype html>
<html lang="en">
  <head>
    <link rel="sitemap" href="/sitemap-index.xml" />
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.jpeg" />
    <meta name="generator" content={Astro.generator} />
    <link
      href="https://fonts.cdnfonts.com/css/helvetica-neue-55"
      rel="stylesheet"
    />
    <title>{title}</title>

    <!-- SEO -->
    <meta name="description" content={description} />
    <link rel="canonical" href={fullCanonicalUrl} />

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={fullCanonicalUrl} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={new URL(image, BASE_ORIGIN)} />

    <!-- Twitter -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:url" content={fullCanonicalUrl} />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={new URL(image, BASE_ORIGIN)} />

    <!-- LinkedIn -->
    <meta property="og:site_name" content="Flitter" />
    <meta name="linkedin:title" content={title} />
    <meta name="linkedin:description" content={description} />
    <meta name="linkedin:image" content={new URL(image, BASE_ORIGIN)} />

    <!-- Pinterest -->
    <meta name="pinterest:title" content={title} />
    <meta name="pinterest:description" content={description} />
    <meta name="pinterest:image" content={new URL(image, BASE_ORIGIN)} />

    <!-- Google tag (gtag.js) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-DY0NVVYBLS"
    ></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag() {
        dataLayer.push(arguments);
      }
      gtag("js", new Date());

      gtag("config", "G-DY0NVVYBLS");
    </script>
    <script is:inline set:html={themeScript}></script>
  </head>
  <body>
    <Header />
    <div class="layout">
      <slot />
    </div>
    <slot name="footer" />
  </body>
</html>

<style is:global>
  html,
  body {
    padding: 0 !important;
    margin: 0 !important;
  }
  body {
    font-family: Helvetica, sans-serif;
    background: #121212 !important;
    color: white !important;
    --header: 64px;
    overflow-y: scroll;
    --px: 64px;
    --mw: 1920px;
  }
  body.dark {
    background: white !important;
    color: black !important; 
  }
  .layout {
    display: flex;
    flex-direction: column;
  }
  @media (max-width: 768px) {
    body {
      --header: 64px;
      --px: 24px;
    }
  }
  /* Chrome, Edge, Safari에서 스크롤바 스타일링 */
  ::-webkit-scrollbar {
    width: 12px;
  }
  ::-webkit-scrollbar-track {
    background: transparent;
  }
  ::-webkit-scrollbar-thumb {
    background-color: #333333;
    border-radius: 20px;
    border: 3px solid #121212;
  }
  ::-webkit-scrollbar-thumb:hover {
    background: #555555;
  }
  code {
    font-family:
      Menlo,
      Monaco,
      Lucida Console,
      Liberation Mono,
      DejaVu Sans Mono,
      Bitstream Vera Sans Mono,
      Courier New,
      monospace;
  }
</style>
