---
import Layout from './Layout.astro'
interface Props {
  title?: string;
  description?: string;
  image?: string;
}

const { title } = Astro.props;

---

<Layout title={title}>
  <slot />
</Layout>

