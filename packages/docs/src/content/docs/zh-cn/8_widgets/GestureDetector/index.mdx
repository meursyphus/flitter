---
nav_group: Widgets
nav_group_order: 3
nav_title: GestureDetector
title: "GestureDetector"
description: "检测用户手势和鼠标事件的组件"
---

## 概述

GestureDetector 是一个检测用户手势和鼠标事件的组件。

GestureDetector 包装子组件以检测和处理各种用户交互事件。它提供点击、鼠标移动和拖拽等各种事件处理器来实现交互式 UI。事件仅在子组件区域内被检测。

<iframe
  width="560"
  height="315"
  src="https://www.youtube.com/embed/WhVXkCFPmK4"
  title="YouTube video player"
  frameborder="0"
  allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
  allowfullscreen
></iframe>

参考: [https://api.flutter.dev/flutter/widgets/GestureDetector-class.html](https://api.flutter.dev/flutter/widgets/GestureDetector-class.html)

## 何时使用？

- 当为按钮或卡片添加点击事件时
- 当实现拖放功能时
- 当应用悬停效果时
- 当创建自定义交互组件时
- 当跟踪鼠标位置时

## 基本用法

```typescript
GestureDetector({
  onClick: (e) => {
    console.log('点击了!');
  },
  child: Container({
    width: 100,
    height: 100,
    color: 'blue',
    child: Center({
      child: Text('点击我')
    })
  })
})
```

## 属性

### onClick

**值: ((e: MouseEvent) => void) | undefined**

鼠标点击时调用的回调函数。

```typescript
GestureDetector({
  onClick: (e) => {
    console.log(`点击位置: ${e.clientX}, ${e.clientY}`);
  },
  child: Text('可点击文本')
})
```

### onMouseDown

**值: ((e: MouseEvent) => void) | undefined**

鼠标按钮按下时调用的回调函数。

```typescript
GestureDetector({
  onMouseDown: (e) => {
    console.log('鼠标按钮按下');
  },
  child: Container({ width: 100, height: 100 })
})
```

### onMouseUp

**值: ((e: MouseEvent) => void) | undefined**

鼠标按钮释放时调用的回调函数。

```typescript
GestureDetector({
  onMouseUp: (e) => {
    console.log('鼠标按钮释放');
  },
  child: Container({ width: 100, height: 100 })
})
```

### onMouseMove

**值: ((e: MouseEvent) => void) | undefined**

鼠标移动时调用的回调函数。

```typescript
GestureDetector({
  onMouseMove: (e) => {
    console.log(`鼠标位置: ${e.clientX}, ${e.clientY}`);
  },
  child: Container({ width: 200, height: 200 })
})
```

### onMouseEnter

**值: ((e: MouseEvent) => void) | undefined**

鼠标进入组件区域时调用的回调函数。

```typescript
GestureDetector({
  onMouseEnter: (e) => {
    console.log('鼠标进入');
  },
  child: Container({ color: 'gray' })
})
```

### onMouseLeave

**值: ((e: MouseEvent) => void) | undefined**

鼠标离开组件区域时调用的回调函数。

```typescript
GestureDetector({
  onMouseLeave: (e) => {
    console.log('鼠标离开');
  },
  child: Container({ color: 'gray' })
})
```

### onMouseOver

**值: ((e: MouseEvent) => void) | undefined**

鼠标在组件上方时调用的回调函数。

```typescript
GestureDetector({
  onMouseOver: (e) => {
    console.log('鼠标悬停');
  },
  child: Container({ width: 100, height: 100 })
})
```

### onDragStart

**值: ((e: MouseEvent) => void) | undefined**

拖拽开始时调用的回调函数。与 onMouseDown 事件一起发生。

```typescript
GestureDetector({
  onDragStart: (e) => {
    console.log('拖拽开始');
  },
  child: Container({ width: 50, height: 50, color: 'red' })
})
```

### onDragMove

**值: ((e: MouseEvent) => void) | undefined**

拖拽过程中调用的回调函数。

```typescript
let position = { x: 0, y: 0 };

GestureDetector({
  onDragMove: (e) => {
    position.x += e.movementX;
    position.y += e.movementY;
    console.log(`拖拽位置: ${position.x}, ${position.y}`);
  },
  child: Container({ width: 50, height: 50 })
})
```

### onDragEnd

**值: ((e: MouseEvent) => void) | undefined**

拖拽结束时调用的回调函数。

```typescript
GestureDetector({
  onDragEnd: (e) => {
    console.log('拖拽结束');
  },
  child: Container({ width: 50, height: 50 })
})
```

### onWheel

**值: ((e: WheelEvent) => void) | undefined**

使用鼠标滚轮时调用的回调函数。

```typescript
GestureDetector({
  onWheel: (e) => {
    console.log(`滚轮增量: ${e.deltaY}`);
    e.preventDefault(); // 阻止默认滚动
  },
  child: Container({ width: 200, height: 200 })
})
```

### cursor

**值: Cursor**（默认值: **"pointer"**）

指定鼠标光标形状。

可用光标类型：
- `"pointer"`: 手形指针（默认）
- `"default"`: 默认箭头
- `"move"`: 移动光标
- `"text"`: 文本选择光标
- `"wait"`: 等待光标
- `"help"`: 帮助光标
- `"crosshair"`: 十字光标
- `"grab"`: 抓取光标
- `"grabbing"`: 抓取中光标
- `"not-allowed"`: 禁止光标
- 调整大小光标: `"n-resize"`, `"e-resize"`, `"s-resize"`, `"w-resize"` 等

```typescript
class DraggableCursor extends StatefulWidget {
  createState() {
    return new DraggableCursorState();
  }
}

class DraggableCursorState extends State<DraggableCursor> {
  cursor = 'grab';
  
  build() {
    return GestureDetector({
      cursor: this.cursor,
      onDragStart: () => this.setState(() => this.cursor = 'grabbing'),
      onDragEnd: () => this.setState(() => this.cursor = 'grab'),
      child: Container({ width: 100, height: 100 })
    });
  }
}
```

### child

**值: Widget | undefined**

要检测事件的子组件。

```typescript
GestureDetector({
  onClick: () => console.log('点击了!'),
  child: Container({
    padding: EdgeInsets.all(20),
    color: 'blue',
    child: Text('可点击区域')
  })
})
```

## 实际示例

### 示例 1: 交互式按钮

```typescript
class InteractiveButton extends StatefulWidget {
  createState() {
    return new InteractiveButtonState();
  }
}

class InteractiveButtonState extends State<InteractiveButton> {
  isPressed = false;
  isHovered = false;
  
  build() {
    return GestureDetector({
      onMouseDown: () => this.setState(() => this.isPressed = true),
      onMouseUp: () => this.setState(() => this.isPressed = false),
      onMouseEnter: () => this.setState(() => this.isHovered = true),
      onMouseLeave: () => {
        this.setState(() => {
          this.isHovered = false;
          this.isPressed = false;
        });
      },
      onClick: () => console.log('按钮被点击!'),
      cursor: 'pointer',
      child: Container({
        padding: EdgeInsets.symmetric({ horizontal: 24, vertical: 12 }),
        decoration: BoxDecoration({
          color: this.isPressed ? '#1976D2' : (this.isHovered ? '#2196F3' : '#42A5F5'),
          borderRadius: BorderRadius.circular(8),
          boxShadow: this.isPressed ? [] : [
            BoxShadow({
              color: 'rgba(0, 0, 0, 0.2)',
              blurRadius: 4,
              offset: { x: 0, y: 2 }
            })
          ]
        }),
        child: Text('自定义按钮', {
          style: TextStyle({ color: 'white', fontWeight: 'bold' })
        })
      })
    });
  }
}
```

### 示例 2: 可拖拽元素

```typescript
class DraggableBox extends StatefulWidget {
  createState() {
    return new DraggableBoxState();
  }
}

class DraggableBoxState extends State<DraggableBox> {
  position = { x: 0, y: 0 };
  isDragging = false;
  
  build() {
    return Transform({
      transform: Matrix4.translation(this.position.x, this.position.y, 0),
      child: GestureDetector({
        onDragStart: () => this.setState(() => this.isDragging = true),
        onDragMove: (e) => {
          this.setState(() => {
            this.position = {
              x: this.position.x + e.movementX,
              y: this.position.y + e.movementY
            };
          });
        },
        onDragEnd: () => this.setState(() => this.isDragging = false),
        cursor: this.isDragging ? 'grabbing' : 'grab',
        child: Container({
          width: 100,
          height: 100,
          decoration: BoxDecoration({
            color: this.isDragging ? 'orange' : 'blue',
            borderRadius: BorderRadius.circular(8),
            boxShadow: [
              BoxShadow({
                color: 'rgba(0, 0, 0, 0.3)',
                blurRadius: this.isDragging ? 8 : 4,
                offset: { x: 0, y: this.isDragging ? 4 : 2 }
              })
            ]
          }),
          child: Center({
            child: Text('拖拽我', {
              style: TextStyle({ color: 'white' })
            })
          })
        })
      })
    });
  }
}
```

### 示例 3: 鼠标跟踪器

```typescript
class MouseTracker extends StatefulWidget {
  createState() {
    return new MouseTrackerState();
  }
}

class MouseTrackerState extends State<MouseTracker> {
  mousePosition = { x: 0, y: 0 };
  isInside = false;
  
  build() {
    return GestureDetector({
      onMouseMove: (e) => {
        const rect = e.currentTarget.getBoundingClientRect();
        this.setState(() => {
          this.mousePosition = {
            x: e.clientX - rect.left,
            y: e.clientY - rect.top
          };
        });
      },
      onMouseEnter: () => this.setState(() => this.isInside = true),
      onMouseLeave: () => this.setState(() => this.isInside = false),
      child: Container({
        width: 300,
        height: 200,
        color: 'lightgray',
        child: Stack({
          children: [
            if (this.isInside) Positioned({
              left: this.mousePosition.x - 10,
              top: this.mousePosition.y - 10,
              child: Container({
                width: 20,
                height: 20,
                decoration: BoxDecoration({
                  color: 'red',
                  borderRadius: BorderRadius.circular(10)
                })
              })
            }),
            Center({
              child: Text(`鼠标位置: ${Math.round(this.mousePosition.x)}, ${Math.round(this.mousePosition.y)}`)
            })
          ]
        })
      })
    });
  }
}
```

### 示例 4: 上下文菜单

```typescript
class ContextMenuExample extends StatefulWidget {
  createState() {
    return new ContextMenuExampleState();
  }
}

class ContextMenuExampleState extends State<ContextMenuExample> {
  showMenu = false;
  menuPosition = { x: 0, y: 0 };
  
  build() {
    return Stack({
      children: [
        GestureDetector({
          onClick: (e) => {
            e.preventDefault();
            if (e.button === 2) { // Right click
              this.setState(() => {
                this.showMenu = true;
                this.menuPosition = { x: e.clientX, y: e.clientY };
              });
            } else {
              this.setState(() => this.showMenu = false);
            }
          },
          child: Container({
            width: 400,
            height: 300,
            color: 'white',
            child: Center({
              child: Text('右键点击打开菜单')
            })
          })
        }),
        if (this.showMenu) Positioned({
          left: this.menuPosition.x,
          top: this.menuPosition.y,
          child: Container({
            width: 150,
            padding: EdgeInsets.symmetric({ vertical: 8 }),
            decoration: BoxDecoration({
              color: 'white',
              borderRadius: BorderRadius.circular(4),
              boxShadow: [
                BoxShadow({
                  color: 'rgba(0, 0, 0, 0.2)',
                  blurRadius: 8,
                  offset: { x: 0, y: 2 }
                })
              ]
            }),
            child: Column({
              children: ['复制', '粘贴', '删除'].map(item =>
                GestureDetector({
                  onClick: () => {
                    console.log(`${item} 被选中`);
                    this.setState(() => this.showMenu = false);
                  },
                  cursor: 'pointer',
                  child: Container({
                    padding: EdgeInsets.symmetric({ horizontal: 16, vertical: 8 }),
                    child: Text(item)
                  })
                })
              )
            })
          })
        })
      ]
    });
  }
}
```

### 示例 5: 可缩放图像

```typescript
class ZoomableImage extends StatefulWidget {
  src: string;
  
  constructor({ src }: { src: string }) {
    super();
    this.src = src;
  }
  
  createState() {
    return new ZoomableImageState();
  }
}

class ZoomableImageState extends State<ZoomableImage> {
  scale = 1;
  
  build() {
    return GestureDetector({
      onWheel: (e) => {
        e.preventDefault();
        const delta = e.deltaY > 0 ? 0.9 : 1.1;
        this.setState(() => {
          this.scale = Math.max(0.5, Math.min(3, this.scale * delta));
        });
      },
      cursor: this.scale > 1 ? 'zoom-out' : 'zoom-in',
      child: Container({
        width: 400,
        height: 300,
        clipped: true,
        child: Transform({
          transform: Matrix4.identity().scaled(this.scale, this.scale, 1),
          alignment: Alignment.center,
          child: Image({ src: this.widget.src, fit: 'cover' })
        })
      })
    });
  }
}
```

## MouseEvent 对象

所有鼠标事件处理器都接收一个标准的 MouseEvent 对象：

- **clientX, clientY**: 相对于视口的鼠标位置
- **pageX, pageY**: 相对于页面的鼠标位置
- **screenX, screenY**: 相对于屏幕的鼠标位置
- **movementX, movementY**: 与前一个事件的移动距离
- **button**: 按下的按钮（0: 左键, 1: 中键, 2: 右键）
- **buttons**: 当前按下按钮的位掩码
- **ctrlKey, shiftKey, altKey, metaKey**: 修饰键状态
- **currentTarget**: 事件发生的元素
- **preventDefault()**: 阻止默认操作
- **stopPropagation()**: 阻止事件冒泡

## 重要说明

- 没有子组件时不会检测事件。
- 事件仅在子组件的实际渲染区域内被检测。
- 拖拽事件是全局跟踪的，所以即使鼠标离开组件也会继续。
- onDragStart 与 onMouseDown 一起发生，处理两个事件时要小心。
- 使用 e.preventDefault() 来阻止浏览器默认行为。

## 相关组件

- **InkWell**: 具有 Material Design 涟漪效果的触摸响应组件
- **Draggable**: 专门用于拖放的组件
- **MouseRegion**: 跟踪鼠标悬停状态的组件
- **Listener**: 处理低级指针事件的组件