---
nav_title: "自定义 RenderObject 教程"
title: "构建自定义 RenderObject"
description: "学习如何通过继承 RenderObjectWidget 来实现自定义组件，同时理解布局和绘制行为"
---

## RenderObjectWidget 实践

现在让我们使用之前学习的 RenderObject 和约束系统来实际创建组件。我们将实现一个简化版本的 Column 来理解 Flitter 的内部工作原理。

## RenderObjectWidget 的类型

**SingleChildRenderObjectWidget**
- 具有单个子节点的组件（Container、Padding 等）
- `createRenderObject()`：创建 RenderObject
- `updateRenderObject()`：更新属性

**MultiChildRenderObjectWidget**
- 具有多个子节点的组件（Row、Column、Stack 等）
- 管理子节点列表
- 协调每个子节点的布局

### 何时使用？

- 当需要现有组件无法实现的自定义布局时
- 当需要性能关键的复杂渲染逻辑时
- 当需要特殊的绘制行为时

## 实现 SimpleColumn

### 定义 Widget 类

```typescript
class SimpleColumn extends MultiChildRenderObjectWidget {
  constructor(props: { children: Widget[] }) {
    super(props);
  }
  
  createRenderObject(): RenderObject {
    return new RenderSimpleColumn();
  }
  
  updateRenderObject(renderObject: RenderSimpleColumn) {
    // 当属性改变时更新 RenderObject
    // 本例没有特殊属性，所以留空
  }
}

export default function SimpleColumn(props: { children: Widget[] }): Widget {
  return new _SimpleColumn(props);
}
```

### RenderObject 类实现

```typescript
class RenderSimpleColumn extends RenderBox {
  performLayout() {
    let height = 0;
    let maxWidth = 0;
    
    // 对每个子节点执行布局
    for (const child of this.children) {
      // 向子节点传递约束（放松高度约束）
      child.layout(constraints.loosen({ height: true }));
      
      // 设置子节点位置（按顺序垂直排列）
      child.offset = new Offset(0, height);
      
      // 计算下一个子节点位置
      height += child.size.height;
      maxWidth = Math.max(maxWidth, child.size.width);
    }
    
    // 确定自身尺寸
    this.size = constraints.constrain(new Size(maxWidth, height));
  }
  
  // 子节点命中测试（点击事件等）
  hitTestChildren(result: HitTestResult, position: Offset): boolean {
    return this.defaultHitTestChildren(result, position);
  }
  
  // 绘制子节点
  paint(context: PaintingContext, offset: Offset) {
    this.defaultPaint(context, offset);
  }
}
```

### 详细布局过程分析

1. **约束传递**：
   ```typescript
   // 从父节点传递约束，仅放松高度约束
   child.layout(constraints.loosen({ height: true }));
   ```

2. **位置计算**：
   ```typescript
   // 通过累加前面子节点的高度来确定 Y 位置
   child.offset = new Offset(0, height);
   height += child.size.height;
   ```

3. **尺寸确定**：
   ```typescript
   // 最宽子节点的宽度和所有子节点高度之和
   this.size = constraints.constrain(new Size(maxWidth, height));
   ```

## 与实际 Column 的差异

实际的 Column 提供更复杂的功能：

### MainAxisAlignment
```typescript
enum MainAxisAlignment {
  start,    // 对齐到开始位置
  center,   // 对齐到中心  
  end,      // 对齐到结束位置
  spaceBetween,  // 均匀分布
  spaceAround,   // 包含周围空间
  spaceEvenly    // 完全均匀分布
}
```

### CrossAxisAlignment
```typescript
enum CrossAxisAlignment {
  start,    // 交叉轴开始位置
  center,   // 交叉轴中心
  end,      // 交叉轴结束位置
  stretch   // 填充整个交叉轴
}
```

### Flex 子节点处理
- **Expanded**：占用剩余空间
- **Flexible**：只占用所需空间

### 溢出处理
- 处理子节点总尺寸超过父节点的情况
- 提供裁剪或滚动功能

## 创建绘制组件

您还可以在 RenderObject 中直接执行绘制操作。Flitter 使用类似 DecoratedBox 的基于类的 Painter 模式：

```typescript
class RenderCustomShape extends RenderBox {
  constructor() {
    super({ isPainter: true }); // 表明这是一个绘制器
  }
  
  performLayout() {
    this.size = constraints.biggest;
  }
  
  // 创建 SVG 绘制器
  protected override createSvgPainter() {
    return new CustomShapeSvgPainter(this);
  }
  
  // 创建 Canvas 绘制器
  protected override createCanvasPainter() {
    return new CustomShapeCanvasPainter(this);
  }
}
```

### SVG 绘制器实现

```typescript
class CustomShapeSvgPainter extends SvgPainter {
  protected override createDefaultSvgEl({ createSvgEl }: SvgPaintContext) {
    return {
      shape: createSvgEl("path"),
      border: createSvgEl("path"),
    };
  }
  
  protected override performPaint(svgEls: { shape: SVGPathElement; border: SVGPathElement }) {
    const { shape, border } = svgEls;
    
    // 生成 SVG 路径数据
    const pathData = `M 10,10 L ${this.size.width-10},10 L ${this.size.width-10},${this.size.height-10} L 10,${this.size.height-10} Z`;
    
    shape.setAttribute('d', pathData);
    shape.setAttribute('fill', '#3b82f6');
    
    border.setAttribute('d', pathData);
    border.setAttribute('fill', 'none');
    border.setAttribute('stroke', '#1e40af');
    border.setAttribute('stroke-width', '2');
  }
}
```

### Canvas 绘制器实现

```typescript
class CustomShapeCanvasPainter extends CanvasPainter {
  override performPaint(context: CanvasPaintingContext, offset: Offset) {
    const ctx = context.canvas;
    
    ctx.save();
    ctx.translate(offset.x, offset.y);
    
    // 绘制矩形
    ctx.fillStyle = '#3b82f6';
    ctx.fillRect(10, 10, this.size.width - 20, this.size.height - 20);
    
    // 绘制边框
    ctx.strokeStyle = '#1e40af';
    ctx.lineWidth = 2;
    ctx.strokeRect(10, 10, this.size.width - 20, this.size.height - 20);
    
    ctx.restore();
  }
}
```

### 需要简单绘制？

与复杂的 RenderObject 实现相比，使用 [CustomPaint 组件](/docs/widgets/CustomPaint) 更简单：

```typescript
CustomPaint({
  painter: {
    svg: {
      createDefaultSvgEl: (context) => ({
        shape: context.createSvgEl('rect')
      }),
      paint: (els, size) => {
        els.shape.setAttribute('width', `${size.width}`);
        els.shape.setAttribute('height', `${size.height}`);
        els.shape.setAttribute('fill', '#3b82f6');
      }
    },
    canvas: {
      paint: (context, size) => {
        context.canvas.fillStyle = '#3b82f6';
        context.canvas.fillRect(0, 0, size.width, size.height);
      }
    }
  }
})
```

## 源代码参考

要参考实际实现，请查看这些文件：

- `packages/flitter/src/component/Column.ts`：实际的 Column 实现
- `packages/flitter/src/renderobject/RenderFlex.ts`：Flex 布局逻辑
- `packages/flitter/src/renderobject/RenderBox.ts`：RenderBox 基类
- `packages/flitter/src/widget/MultiChildRenderObjectWidget.ts`：多子节点组件

## 关键总结

1. **RenderObjectWidget 连接 Widget 和 RenderObject**
2. **performLayout() 确定子节点的尺寸和位置**
3. **paint() 执行实际的绘制操作**
4. **必须同时支持 SVG 和 Canvas 渲染器**
5. **只需要实现所需的功能**

在下一章中，我们将学习简化这些复杂操作的 CustomPaint 组件。