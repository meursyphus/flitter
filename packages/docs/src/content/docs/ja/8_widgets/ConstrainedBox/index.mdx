---
nav_group: Widgets
nav_group_order: 3
nav_title: ConstrainedBox
title: "ConstrainedBox"
description: "子ウィジェットに追加制約を課すウィジェット"
---

## 概要

ConstrainedBoxは、子ウィジェットに追加の制約を課すウィジェットです。親からの制約と独自に定義した制約を組み合わせて、子のサイズを制御します。

<iframe
  width="560"
  height="315"
  src="https://www.youtube.com/embed/o2KveVr7adg"
  title="YouTube video player"
  frameborder="0"
  allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
  allowfullscreen
></iframe>

Reference: [https://api.flutter.dev/flutter/widgets/ConstrainedBox-class.html](https://api.flutter.dev/flutter/widgets/ConstrainedBox-class.html)

## 使用場面

- ウィジェットの最小・最大サイズを制限したい場合
- 子ウィジェットのサイズを特定の範囲内に制限したい場合
- レスポンシブレイアウトでサイズ制約が必要な場合
- スクロール可能エリアで最小高さを確保したい場合
- 画像や動画の最大サイズを制限したい場合

## 基本的な使用方法

```typescript
// 最小サイズの設定
ConstrainedBox({
  constraints: new Constraints({
    minWidth: 100,
    minHeight: 100
  }),
  child: Container({
    color: 'blue'
  })
})

// 最大サイズの設定
ConstrainedBox({
  constraints: new Constraints({
    maxWidth: 200,
    maxHeight: 200
  }),
  child: Container({
    color: 'red',
    width: 300,  // 200に制限される
    height: 300  // 200に制限される
  })
})

// 固定サイズの設定
ConstrainedBox({
  constraints: Constraints.tight({ width: 150, height: 150 }),
  child: Container({
    color: 'green'
  })
})
```

## プロパティ

### constraints（必須）
**型: Constraints**

子ウィジェットに課す制約。Constraintsには以下のプロパティがあります：

- `minWidth`: 最小幅（デフォルト: 0）
- `maxWidth`: 最大幅（デフォルト: Infinity）
- `minHeight`: 最小高さ（デフォルト: 0）
- `maxHeight`: 最大高さ（デフォルト: Infinity）

```typescript
// 様々な制約の例
ConstrainedBox({
  constraints: new Constraints({
    minWidth: 100,
    maxWidth: 300,
    minHeight: 50,
    maxHeight: 200
  }),
  child: child
})
```

### child（オプション）
**型: Widget | undefined**

制約が適用される子ウィジェット。

## 制約のコンストラクタ

Constraintsはいくつかの便利なコンストラクタを提供します：

```typescript
// デフォルトコンストラクタ
new Constraints({
  minWidth: 0,
  maxWidth: Infinity,
  minHeight: 0,
  maxHeight: Infinity
})

// タイト制約（固定サイズ）
Constraints.tight({ width: 100, height: 100 })

// ルーズ制約（最大サイズのみ制限）
Constraints.loose({ width: 200, height: 200 })

// 拡張（親サイズまで拡張）
Constraints.expand()  // Expand in all directions
Constraints.expand({ width: 300 })  // 幅は300、高さは拡張
Constraints.expand({ height: 400 })  // 高さは400、幅は拡張
```

## 制約の組み合わせ

ConstrainedBox combines parent constraints with its own constraints:

```typescript
// Parent constraints: min(50, 50), max(200, 200)
// ConstrainedBox constraints: min(100, 100), max(150, 150)
// Result: min(100, 100), max(150, 150)

Container({
  constraints: new Constraints({
    minWidth: 50,
    maxWidth: 200,
    minHeight: 50,
    maxHeight: 200
  }),
  child: ConstrainedBox({
    constraints: new Constraints({
      minWidth: 100,
      maxWidth: 150,
      minHeight: 100,
      maxHeight: 150
    }),
    child: Container({ color: 'blue' })
  })
})
```

## 実践的な例

### 例1: ボタンの最小サイズ確保

```typescript
const MinSizeButton = ({ onPressed, child, minWidth = 88, minHeight = 36 }) => {
  return ConstrainedBox({
    constraints: new Constraints({
      minWidth: minWidth,
      minHeight: minHeight
    }),
    child: ElevatedButton({
      onPressed: onPressed,
      child: child
    })
  });
};

// Usage
MinSizeButton({
  onPressed: () => console.log('clicked'),
  child: Text('OK')  // Minimum size ensured even with short text
})
```

### Example 2: Limiting Image Size

```typescript
const ConstrainedImage = ({ src, maxSize = 300 }) => {
  return Container({
    decoration: BoxDecoration({
      border: Border.all({ color: '#E0E0E0' }),
      borderRadius: BorderRadius.circular(8)
    }),
    child: ConstrainedBox({
      constraints: new Constraints({
        maxWidth: maxSize,
        maxHeight: maxSize
      }),
      child: Image({
        src: src,
        objectFit: 'contain'
      })
    })
  });
};
```

### Example 3: Responsive Card

```typescript
const ResponsiveCard = ({ title, content }) => {
  return ConstrainedBox({
    constraints: new Constraints({
      minHeight: 100,
      maxWidth: 400
    }),
    child: Card({
      child: Padding({
        padding: EdgeInsets.all(16),
        child: Column({
          crossAxisAlignment: CrossAxisAlignment.start,
          mainAxisSize: MainAxisSize.min,
          children: [
            Text(title, {
              style: TextStyle({
                fontSize: 20,
                fontWeight: 'bold'
              })
            }),
            SizedBox({ height: 8 }),
            Text(content, {
              style: TextStyle({
                fontSize: 16,
                color: '#666'
              })
            })
          ]
        })
      })
    })
  });
};
```

### Example 4: Minimum Height for Scrollable Area

```typescript
const ScrollableContent = ({ children, minHeight = 300 }) => {
  return SingleChildScrollView({
    child: ConstrainedBox({
      constraints: new Constraints({
        minHeight: minHeight
      }),
      child: Column({
        children: children
      })
    })
  });
};
```

### Example 5: Constrained Box with Aspect Ratio

```typescript
const ConstrainedAspectBox = ({ 
  child, 
  aspectRatio = 16/9, 
  maxWidth = 600 
}) => {
  return ConstrainedBox({
    constraints: new Constraints({
      maxWidth: maxWidth
    }),
    child: AspectRatio({
      aspectRatio: aspectRatio,
      child: Container({
        decoration: BoxDecoration({
          color: '#F5F5F5',
          borderRadius: BorderRadius.circular(12)
        }),
        child: child
      })
    })
  });
};

// Usage example: Video player
ConstrainedAspectBox({
  aspectRatio: 16/9,
  maxWidth: 800,
  child: VideoPlayer({ url: 'video.mp4' })
})
```

## Nested ConstrainedBox

When nesting multiple ConstrainedBoxes, the most restrictive constraint applies:

```typescript
ConstrainedBox({
  constraints: new Constraints({ minWidth: 200, minHeight: 200 }),
  child: ConstrainedBox({
    constraints: new Constraints({ minWidth: 100, minHeight: 100 }),
    child: Container({ color: 'blue' })
  })
})
// Result: Minimum size is 200x200
```

## 重要な注意事項

- Parent constraints always take precedence
- Conflicting constraints result in the most restrictive being applied
- With infinite size constraints, the child's intrinsic size is used
- Too much nesting can impact layout performance
- Tight constraints completely ignore the child's size

## 関連ウィジェット

- **Container**: Provides the same functionality with constraints property
- **SizedBox**: Simple way to set fixed size
- **LimitedBox**: Limits size only in infinite constraints
- **UnconstrainedBox**: Removes constraints from child
- **OverflowBox**: Allows child to exceed parent constraints