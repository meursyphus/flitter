---
nav_group: Widgets
nav_group_order: 3
nav_title: Column
title: "Column"
description: "子要素を垂直に配置するレイアウトウィジェット"
---

## 概要

Columnは、子要素を垂直方向に配置するレイアウトウィジェットです。

Columnは、方向が垂直に固定されたFlexウィジェットの便利なラッパーです。複数のウィジェットを上から下に配置するために使用され、様々な配置オプションを提供します。デフォルトでは、Columnは利用可能なすべての垂直スペースを占めますが、`mainAxisSize`を調整して必要な分だけのスペースを占めるようにできます。

## 使用場面

- フォームフィールドを垂直に配置する場合
- リストアイテム内のコンテンツを構造化する場合
- カードやダイアログの垂直レイアウトを作成する場合
- メニューアイテムを垂直に配置する場合
- タイトル、サブタイトル、本文を順番に表示する場合

## 基本的な使用方法

```typescript
// Simple example
const column = Column({
  children: [
    Text('Title'),
    Text('Subtitle'),
    Text('Body')
  ]
});

// Using alignment options
const alignedColumn = Column({
  mainAxisAlignment: MainAxisAlignment.center,
  crossAxisAlignment: CrossAxisAlignment.start,
  children: [
    Text('Left aligned'),
    Text('Vertically centered'),
    Text('Text widgets')
  ]
});
```

## プロパティ

### children（必須）

**型: Widget[]**

Columnに表示する子ウィジェットの配列。配列の順序で上から下に配置されます。

```typescript
Column({
  children: [
    Container({ width: 100, height: 50, color: 'red' }),
    Container({ width: 100, height: 50, color: 'green' }),
    Container({ width: 100, height: 50, color: 'blue' })
  ]
})
```

### mainAxisAlignment

**Value: MainAxisAlignment** (default: **MainAxisAlignment.start**)

Defines the vertical alignment of child widgets.

- **start**: Top alignment (default)
- **end**: Bottom alignment
- **center**: Center alignment
- **spaceBetween**: Place first and last widgets at edges, distribute remaining space evenly
- **spaceAround**: Distribute equal space around each widget
- **spaceEvenly**: Distribute equal space between all widgets and edges

```typescript
// Center alignment
Column({
  mainAxisAlignment: MainAxisAlignment.center,
  children: [
    Text('A'),
    Text('B'),
    Text('C')
  ]
})

// Space evenly
Column({
  mainAxisAlignment: MainAxisAlignment.spaceEvenly,
  children: [
    Icon({ icon: Icons.home }),
    Icon({ icon: Icons.search }),
    Icon({ icon: Icons.settings })
  ]
})
```

### crossAxisAlignment

**Value: CrossAxisAlignment** (default: **CrossAxisAlignment.center**)

Defines the horizontal alignment of child widgets.

- **start**: Left alignment
- **end**: Right alignment
- **center**: Horizontal center alignment (default)
- **stretch**: Stretch to fill available horizontal space

```typescript
Column({
  crossAxisAlignment: CrossAxisAlignment.start,
  children: [
    Container({ width: 100, height: 50, color: 'red' }),
    Container({ width: 150, height: 50, color: 'green' }),
    Container({ width: 75, height: 50, color: 'blue' })
  ]
})
```

### mainAxisSize

**Value: MainAxisSize** (default: **MainAxisSize.max**)

Controls the amount of vertical space the Column takes up.

- **max**: Take up all available vertical space (default)
- **min**: Take up only the minimum space needed by children

```typescript
// Take up only necessary space
Container({
  color: 'lightgray',
  child: Column({
    mainAxisSize: MainAxisSize.min,
    children: [
      Text('Compact'),
      Text('Column')
    ]
  })
})
```

### verticalDirection

**Value: VerticalDirection** (default: **VerticalDirection.down**)

Defines the vertical layout order of child widgets.

- **down**: From top to bottom (default)
- **up**: From bottom to top

```typescript
Column({
  verticalDirection: VerticalDirection.up,
  children: [
    Text('1st'), // Displayed at bottom
    Text('2nd'), // Displayed in middle
    Text('3rd')  // Displayed at top
  ]
})
```

## 高度な使用方法

### Expandedとの併用

```typescript
Column({
  children: [
    Container({ width: 100, height: 50, color: 'red' }),
    Expanded({
      child: Container({ width: 100, color: 'green' })
    }),
    Container({ width: 100, height: 50, color: 'blue' })
  ]
})
// The green Container takes up all remaining vertical space
```

### 比例配分スペース配分

```typescript
Column({
  children: [
    Flexible({
      flex: 1,
      child: Container({ color: 'red' })
    }),
    Flexible({
      flex: 2,
      child: Container({ color: 'green' })
    }),
    Flexible({
      flex: 1,
      child: Container({ color: 'blue' })
    })
  ]
})
// Red takes 25%, green takes 50%, blue takes 25% of height
```

## 実用的な例

### 例1: ログインフォーム

```typescript
const loginForm = Container({
  padding: EdgeInsets.all(20),
  child: Column({
    mainAxisAlignment: MainAxisAlignment.center,
    crossAxisAlignment: CrossAxisAlignment.stretch,
    children: [
      Text('Login', {
        style: TextStyle({ fontSize: 24, fontWeight: 'bold' })
      }),
      SizedBox({ height: 30 }),
      TextField({
        decoration: InputDecoration({
          labelText: 'Email',
          border: OutlineInputBorder()
        })
      }),
      SizedBox({ height: 16 }),
      TextField({
        obscureText: true,
        decoration: InputDecoration({
          labelText: 'Password',
          border: OutlineInputBorder()
        })
      }),
      SizedBox({ height: 24 }),
      ElevatedButton({
        onPressed: () => console.log('Login'),
        child: Text('Login')
      }),
      SizedBox({ height: 16 }),
      TextButton({
        onPressed: () => console.log('Sign up'),
        child: Text("Don't have an account? Sign up")
      })
    ]
  })
});
```

### 例2: カードコンテンツ

```typescript
const card = Container({
  margin: EdgeInsets.all(16),
  padding: EdgeInsets.all(16),
  decoration: BoxDecoration({
    color: 'white',
    borderRadius: BorderRadius.circular(12),
    boxShadow: [
      BoxShadow({
        color: 'rgba(0, 0, 0, 0.1)',
        blurRadius: 10,
        offset: { x: 0, y: 4 }
      })
    ]
  }),
  child: Column({
    crossAxisAlignment: CrossAxisAlignment.start,
    children: [
      Image({
        src: 'product.jpg',
        height: 200,
        fit: 'cover'
      }),
      SizedBox({ height: 16 }),
      Text('Product Name', {
        style: TextStyle({ fontSize: 20, fontWeight: 'bold' })
      }),
      SizedBox({ height: 8 }),
      Text('Product description goes here...', {
        style: TextStyle({ color: 'gray' })
      }),
      SizedBox({ height: 16 }),
      Row({
        mainAxisAlignment: MainAxisAlignment.spaceBetween,
        children: [
          Text('$25.00', {
            style: TextStyle({ fontSize: 18, fontWeight: 'bold', color: 'blue' })
          }),
          IconButton({
            icon: Icons.favorite_border,
            onPressed: () => {}
          })
        ]
      })
    ]
  })
});
```

### 例3: 設定メニュー

```typescript
const settingsMenu = Column({
  children: [
    ListTile({
      leading: Icon({ icon: Icons.person }),
      title: Text('Profile'),
      trailing: Icon({ icon: Icons.arrow_forward_ios }),
      onTap: () => console.log('Profile')
    }),
    Divider(),
    ListTile({
      leading: Icon({ icon: Icons.notifications }),
      title: Text('Notifications'),
      trailing: Switch({
        value: true,
        onChanged: (value) => {}
      })
    }),
    Divider(),
    ListTile({
      leading: Icon({ icon: Icons.lock }),
      title: Text('Privacy'),
      trailing: Icon({ icon: Icons.arrow_forward_ios }),
      onTap: () => console.log('Privacy')
    }),
    Divider(),
    ListTile({
      leading: Icon({ icon: Icons.help }),
      title: Text('Help'),
      trailing: Icon({ icon: Icons.arrow_forward_ios }),
      onTap: () => console.log('Help')
    })
  ]
});
```

## 重要な注意事項

- 子ウィジェットが垂直スペースを超えるとオーバーフローエラーが発生します。そのような場合は`SingleChildScrollView`や`Flexible`/`Expanded`を使用してください。
- `crossAxisAlignment.stretch`を使用する場合、子ウィジェットの幅が制約されていないと無限に拡張する可能性があります。
- Columnはスクロールをサポートしません。スクロールが必要な場合は`SingleChildScrollView`や`ListView`を使用してください。
- 水平レイアウトには`Row`ウィジェットを使用してください。

## 関連ウィジェット

- **Row**: 子要素を水平に配置
- **Flex**: ColumnとRowのベースとなるより柔軟なレイアウトウィジェット
- **ListView**: スクロール可能な垂直リスト
- **Stack**: 子要素を上下に重ねて表示
- **Expanded**: Column内の残りスペースを占有
- **Flexible**: Column内で柔軟なサイズ調整を可能に